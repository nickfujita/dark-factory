# References

`feature-map-example/` is the worked example the generator follows: a README index and one
file per feature, each carrying the four required H2 sections (Sub-features, How to get to it
(user POV), Driving it with the harness, Gotchas). The generated map lives in the target
repo, not here.
