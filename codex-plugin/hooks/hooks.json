{
  "description": "Dark Factory session hook for Codex. Prints the df reminder at session start and names the resolved dark-factory root so skills can find scripts/ and references/. It does not activate df mode; entry stays the operator invoking $df.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "sh ${CLAUDE_PLUGIN_ROOT}/scripts/df-session-hook.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
